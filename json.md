# json

* from [here](https://www.json.org/)

## structure
basically it's one of these two, nested

### `object`
```
string: value
```

### `array`
```
list of values
[value]
```

where `value` can be
* `string`
* `number`
* `object`
* `array`
* `true`
* `false`
* `null`
